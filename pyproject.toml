[build-system]
requires = ["setuptools>=65.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "engine"
version = "0.3.1"
description = "2D Game Engine using pyglet rendering"
authors = [{ name = "WhiteWolf45380" }]
dependencies = [
    "pyglet",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["engine*"]